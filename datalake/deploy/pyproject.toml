[project]
name = "datalake"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "dagster",
    "dagster-webserver",
    "polars",
    "httpx",
    "boto3",
    "python-dotenv",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:BuildBackend"